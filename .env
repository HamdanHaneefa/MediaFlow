# API Configuration
# Main API URL - Used by admin services (contacts, projects, tasks, etc.)
VITE_API_URL=http://localhost:4000/api
# VITE_API_URL=https://crm-api.elamai.in/api

# Note: Client Portal API will automatically use /api/client
# No separate env variable needed - it's constructed in clientAuth.ts
